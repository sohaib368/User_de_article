
<%= form_for(@comment) do |form| %>

  <%= form.hidden_field(:commentable_id ) %>
  <%= form.hidden_field(:commentable_type)  %>
  <%= form.text_field(:comment_details)%>
  <%= form.submit 'Create' %>
<% end %>
